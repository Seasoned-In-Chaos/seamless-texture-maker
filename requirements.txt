PyQt6>=6.5.0,<7.0.0
opencv-python-headless>=4.8.0,<5.0.0
# Ceiling matches numba's own declared numpy compatibility (numba<0.66
# requires numpy<2.5) -- an unbounded numpy here can resolve a version
# newer than the installed numba supports, breaking JIT compilation.
numpy>=1.24.0,<2.5.0
Pillow>=10.0.0,<13.0.0
pyinstaller>=6.0.0,<7.0.0
numba>=0.58.0,<0.70.0
psutil>=5.9.0,<8.0.0
packaging>=23.0,<27.0
# Supply NVVM + the CUDA Runtime so Numba's @cuda.jit can compile and run
# the optional Splat GPU path (materialize_methods_cuda.py) without
# requiring end users to install NVIDIA's full CUDA Toolkit separately --
# see gpu_utils._ensure_cuda_home() for how Numba is pointed at them.
# Harmless on machines without an NVIDIA GPU: is_numba_cuda_available()
# gates all actual use, so these just sit unused (~50MB total) on
# AMD/Intel/no-GPU installs.
nvidia-cuda-nvcc-cu12>=12.0,<13.0; sys_platform == "win32"
nvidia-cuda-runtime-cu12>=12.0,<13.0; sys_platform == "win32"
